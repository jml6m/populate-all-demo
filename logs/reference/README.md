# Reference trace logs

This directory will hold canonical trace/log artifacts produced by official tagged releases.

Artifacts are organized by versioned subdirectories (`v1/`, `v2/`, ...). Each version directory is immutable after publication.

Unofficial and local runs write to the sibling `logs/local/` directory (gitignored).

Canonical reference artifacts will start being populated with the `v1` release.
